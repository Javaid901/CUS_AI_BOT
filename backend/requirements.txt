fastapi==0.136.3
uvicorn[standard]==0.49.0
# Redis shared-state layer for multi-worker scaling (Phase 3C-5). The app runs
# single-process with REDIS_URL unset; set REDIS_URL to enable the distributed
# state/cache/rate-limit layer.
redis==8.0.1
sqlalchemy==2.0.50
# PostgreSQL driver used by DATABASE_URL=postgresql+psycopg://... (Phase 3C-2).
# SQLite remains the default; this only activates when a PG URL is configured.
psycopg[binary]==3.3.5
# Alembic is the authoritative production schema-migration mechanism (Phase 3C-2).
alembic==1.15.1
pydantic==2.13.4
pydantic-settings==2.14.2
email-validator==2.3.0
python-multipart==0.0.20
python-dotenv==1.2.2
PyJWT==2.13.0
bcrypt==5.0.0
slowapi==0.1.9
chromadb==1.5.9
sentence-transformers==2.7.0
httpx==0.28.1
pypdf==6.14.2
python-docx==1.2.0
reportlab==5.0.0
numpy==2.5.0
